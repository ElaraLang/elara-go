namespace test/lol
import elara/std

let loop(Int times, () => Unit fun) => {
    let mut count = 0
    while count != times {
        fun()
        count = count + 1
    }
}

loop(5, () => { print("Hi") })