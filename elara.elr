namespace test/lol
import elara/std

type Named = {
    String name
}

type Aged = {
    Int age
}
struct Person {
    String name
    Int age
}

type Entity = Named & Aged

let m: { Named : Entity } = 3