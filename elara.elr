namespace test/lol
import elara/std

type Named = {
    String name
}

type Aged = {
    Int age
}
struct Person {
    String name
    Int age
}

type Entity = Named & Aged

let a: [Char] = ['a', 'b', 'c']
print(a)