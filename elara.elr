namespace test/lol
import elara/std

type Named = {
    String name
}

type Aged = {
    Int age
}
struct Person {
    String name
    Int age
}

type Entity = Named & Aged

let test(Entity t) => {
    print(t.name)
}

let dave = Person("Dave", 18)
test(dave)

let hmm(Char c) => c + " "

"Hello".map(hmm).print()