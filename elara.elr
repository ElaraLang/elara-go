namespace test/lol

import elara/std

struct Person {
    String name
    Int age
}

let daveFactory = () => { Person("Dave", 50) }

let produceDaves(Int amount) => {
    let factories = [daveFactory] * amount
    factories.map(run)
}

let daves = produceDaves(100000)

print(daves)